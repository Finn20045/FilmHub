# 🎬 FilmHub

**FilmHub** — это платформа для совместного просмотра фильмов в реальном времени. Создавайте комнаты, приглашайте друзей и наслаждайтесь кино с синхронизацией видео и живым чатом, где бы вы ни находились.

---

## ✨ Основные возможности

### 🎥 Совместный просмотр
*   **Синхронизация:** Видео синхронизируется между всеми участниками комнаты с точностью до секунды. Если кто-то ставит паузу или перематывает — это происходит у всех.
*   **Логика "Late Join":** Если вы зашли в комнату позже, плеер автоматически перемотается к текущему моменту просмотра.

### 💬 Коммуникация
*   **Real-time Чат:** Мгновенные сообщения на базе WebSockets.
*   **Уведомления:** Системные сообщения о входе/выходе участников.
*   **Аватарки:** Отображение аватарок пользователей в чате.

### 🛠 Управление комнатами
*   **Приватность:** Возможность установки пароля на комнату.
*   **Свой контент:** Загрузка собственных видеофайлов при создании комнаты.
*   **Модерация:** Владелец комнаты может выгонять ("кикать") нежелательных участников.
*   **Шеринг:** Быстрое копирование ссылки-приглашения.

### 👤 Пользователи
*   **Профиль:** Редактирование личных данных и загрузка аватара.
*   **Адаптивность:** Полноценная мобильная версия для просмотра с телефона.

---

## 🚀 Технологический стек

### Backend
*   **Python 3.11**
*   **Django 5** & **Django REST Framework (DRF)** — Основная логика и API.
*   **Django Channels** — Работа с WebSockets (асинхронный сервер Daphne).
*   **Redis** — Channel layer для обмена сообщениями между сокетами.

### Frontend
*   **React.js** — SPA приложение.
*   **Axios** — Взаимодействие с API.
*   **React Router** — Навигация.
*   **CSS Modules** — Стилизация (Glassmorphism UI).

### Инфраструктура
*   **Docker & Docker Compose** — Контейнеризация всех сервисов.
*   **Nginx** — Веб-сервер, проксирование запросов и раздача статики/медиа.

---

## 🛠 Установка и запуск (Docker)

Это рекомендуемый способ запуска. Вам понадобятся установленные Docker и Docker Compose.

1.  **Клонируйте репозиторий:**
    ```bash
    git clone https://github.com/Finn20045/FilmHub.git
    cd FilmHub
    ```

2.  **Запустите проект:**
    ```bash
    docker-compose up --build
    ```

3.  **Откройте в браузере:**
    Перейдите по адресу `http://localhost`.

    *Если вы хотите зайти с телефона в одной Wi-Fi сети, узнайте локальный IP компьютера (например, `192.168.1.x`) и введите его в браузере телефона.*

---

## ⚙️ Локальная разработка (Без Docker)

Если вы хотите запустить проект вручную:

### Backend
1.  Перейдите в папку `backend`.
2.  Создайте и активируйте виртуальное окружение:
    ```bash
    python -m venv venv
    # Windows: venv\Scripts\activate
    # Linux/Mac: source venv/bin/activate
    ```
3.  Установите зависимости:
    ```bash
    pip install -r requirements.txt
    ```
4.  Выполните миграции:
    ```bash
    python manage.py migrate
    ```
5.  Запустите сервер:
    ```bash
    python manage.py runserver
    ```

### Frontend
1.  Перейдите в папку `frontend`.
2.  Установите зависимости:
    ```bash
    npm install
    ```
3.  Запустите React:
    ```bash
    npm start
    ```

*Примечание: Для работы WebSockets в локальном режиме убедитесь, что в `settings.py` используется `InMemoryChannelLayer` или у вас запущен локальный Redis.*

---

## 🔐 Админ-панель

Для доступа к управлению фильмами и пользователями:

1.  Создайте суперпользователя (в терминале запущенного контейнера или локально):
    ```bash
    # Если Docker:
    docker-compose exec backend python manage.py createsuperuser

    # Если локально:
    python manage.py createsuperuser
    ```
2.  Зайдите по адресу `http://localhost/admin/`.

---

## 🧹 Очистка старых комнат

В проекте реализована команда для автоматического удаления заброшенных комнат (где не было активности более 24 часов).

Запуск вручную:
```bash
python manage.py cleanup_rooms